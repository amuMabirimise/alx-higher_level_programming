#!/usr/bin/python3

def safe_print_list(my_list=[], x=0):
    try:
        elements = 0
        for i in range(x):
            if count < x:
                print(element, end=' ')
                element += 1
            else:
                break
        print()
        return elements
    except IndexError:
        return elements
